# THIS FILE IS EXCLUSIVELY MAINTAINED IN THE AE ROOT PACKAGE. ANY CHANGES SHOULD BE DONE THERE.
# All changes will be deployed automatically to all the portions of this namespace package.

image: python:3.6


before_script:
  - python --version
  - pip install -r requirements.txt -r test_requirements.txt


pytest:
  # stage: test   # default stage
  script:
    - python -m pytest --cov=ae tests/


flake8:
  # stage: test   # default stage
  script:
    - flake8 --max-line-length=120 ae/*.py

mypy:
  script:
    - python -m mypy ae/*.py

pylint:
  script:
    - pylint --max-line-length=120 ae/*.py
  allow_failure: true       # pylint is too picky but still useful for manual code smell checks


deploy_staging:
  stage: deploy
  variables:
    TWINE_USERNAME: $PYPI_USERNAME
    TWINE_PASSWORD: $PYPI_PASSWORD
  before_script:
     - pip install twine
     - python setup.py sdist
  script:
    - twine upload --verbose dist/*
  only:
    - master
